#include <iostream>

using namespace std;

int main()
{
    int N, K;
    cin >> N, K;
    int table[N][N];
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            cin >> table[i][j];
        }
    }

    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            
        }
    }
}